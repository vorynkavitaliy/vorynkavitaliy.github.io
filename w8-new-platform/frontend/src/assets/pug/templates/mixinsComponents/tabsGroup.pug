mixin TabsGroup(array, classes, dataId)
    ul.tabs-group.grid-container(class=`cols-${array.length} ${classes}`, data-id=dataId)
        each item in array
            li.tab-item
                if (item.active)
                    +Button(item.color, item.active, item.text)
                else
                    +Button(item.color, '', item.text)